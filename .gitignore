# Dependencies
node_modules/

# Monorepo: single lockfile at repo root (do not commit per-package lockfiles)
ai/pnpm-lock.yaml
game/pnpm-lock.yaml

# pnpm local store (when using store-dir or .pnpm-store)
.pnpm-store/

# Build output
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example

# macOS
.DS_Store
